# 🔍 炒股面板系统全面复盘报告

> 生成时间: 2026-07-03 11:55  
> 诊断范围: 数据源 → 后端 → 前端 → WorkBuddy交互 → 面板展示

---

## 一、问题统计

| 层级 | 致命🔴 | 高危🟠 | 中危🟡 | 合计 |
|------|--------|--------|--------|------|
| 🔧 后端 | 4 | 7 | 8 | 19 |
| 🖥️ 前端 | 5 | 7 | 6 | 18 |
| 💾 数据 | 3 | 2 | 1 | 6 |
| 🔗交互层 | 1 | 1 | 1 | 3 |
| **合计** | **13** | **17** | **16** | **46** |

---

## 二、致命问题（Critical — 系统已崩溃）

### C1: 前端5个DOM容器缺失
- **文件**: `static/index.html` 行 820-974
- **现象**: `getElementById('signal-list')` / `skill-list` / `ai-result-panel` / `ai-result-content` / `ai-history-list` 全部返回 `null`
- **后果**: `TypeError: Cannot set properties of null`
- **影响范围**: 信号系统、AI技能系统、分析历史 — 完全失效
- **修复**: 在 `<div id="content">` 内添加对应的 `<div>` 容器

### C2: 变量名拼写错误
- **文件**: `static/index.html` 行 189 vs 548
- **代码**: 声明 `_currentReqId`，使用 `_currentRequestId`（多了"uest"）
- **后果**: `ReferenceError`，`selectBoard` 函数崩溃
- **影响范围**: 用户切换板块时直接白屏

### C3: 后端 `_load_ledger()` / `_save_ledger()` 未定义
- **文件**: `data_update_manager.py` 行 167-170
- **代码**: 
  ```python
  ledger = _load_ledger()   # NameError!
  _save_ledger(ledger)       # NameError!
  ```
- **后果**: `NameError: name '_load_ledger' is not defined`
- **影响范围**: 每日增量更新功能完全瘫痪

### C4: 数据库连接泄漏
- **文件**: `data_loader.py` 行 334-393
- **代码**: 每次调用 `_qmt_spot()` 都 `connect()` 但从不 `disconnect()`
- **后果**: 高并发时QMT服务拒绝连接

### C5: 数据目录有2份不一致拷贝
- **根目录** `行业板块K线数据/` (496文件) vs **board-app/data/** `行业板块K线数据/` (497文件)
- 差异: 同名文件某些行末位小数不同（如换手率 `0.2` vs `0.19`）

### C6: 4个概念板块CSV缺失
- 地下管网(BK1058)、中盘成长(BK1669)、工业母机(BK1004)、医疗器械概念(BK0668)
- 因抓取超时被记录到 `_failed.csv`，没有数据文件

### C7: weekly文件混入日K目录
- `board-app/data/概念板块K线数据/CPO概念_BK1128.weekly.csv` 不应与日K混存

### C8: 搜索索引1.1MB从未加载到前端
- `search_index.json`（78691行）静态存在但从未被 fetch
- `_pinyinData` 始终为空对象 `{}`
- 实际拼音排序功能完全失效

### C9: SSE (EventSource) 无重连机制
- 网络波动断开后所有实时推送永久失效
- 用户需手动刷新页面才能恢复

### C10: Flask SECRET_KEY 未设置
- Session签名无效，CSRF防护不可用

### C11: CORS 全开放
- 任意外部网站可向面板发送请求

### C12: proxy_bypass 全局 monkey-patch + verify=False
- 影响所有requests调用，禁用SSL证书验证
- 中间人攻击风险

### C13: 端口58610硬编码4处
- data_loader.py、kline_service.py、board_service.py 等均硬编码

---

## 三、高危问题（High）

### H1: 请求体无大小限制
- Flask默认无限，可被超大JSON耗尽内存

### H2: QMT 端口硬编码 + PID复用风险
- `_force_free_port()` 直接 `os.kill(pid, 9)`，不验证进程名

### H3: 异常吞没 30+处
- 大量 `except: pass` / `except Exception: # 注释`，无日志

### H4: JSON.parse 无try-catch保护
- 10+处SSE事件中的危险点

### H5: Datafeed缓存无LRU淘汰
- 长期运行内存持续增长

### H6: store.categoryData 为null时崩溃
- `loadClassification` 失败时未设置 `store.categoryData = []`

### H7: CSS 重复定义
- `#search-input` 两处定义导致尺寸不可预测

### H8: 隐式全局变量
- 多变量未声明直接赋值 (`found`, `items`, `col`, `p`, `h`, `id`)

---

## 四、中危问题（Medium）

### M1: `initPro` 被调度两次
### M2: `preloadFirstBoard` 双重调度
### M3: 右键菜单超出视口（无边界检测）
### M4: 右侧面板位置硬编码 `280px`
### M5: 拖动排序后无持久化
### M6: MCP端点为空壳
### M7: 无API版本前缀
### M8: QMT连接未释放

---

## 五、修正方案

### Phase 1：救命（让用户能操作）

| # | 文件 | 操作 | 行数 |
|---|------|------|------|
| 1 | `static/index.html` | 添加5个缺失的DOM容器 | +15 |
| 2 | `static/index.html:548` | `_currentRequestId` → `_currentReqId` | 1 |
| 3 | `data_update_manager.py` | 实现 `_load_ledger()` / `_save_ledger()` | ~30 |
| 4 | `data_loader.py` | QMT连接添加 `disconnect()` | ~20 |

### Phase 2：安全（1天内）

| # | 文件 | 操作 |
|---|------|------|
| 5 | `app.py` | 添加 SECRET_KEY、MAX_CONTENT_LENGTH、CORS限制 |
| 6 | `proxy_bypass.py` | 取消全局 verify=False，限制范围 |
| 7 | `port_manager.py` | SIGTERM → 等待 → SIGKILL，验证进程名 |
| 8 | `static/index.html` | SSE 断线重连 + 指数退避 |

### Phase 3：体验（3天内）

| # | 文件 | 操作 |
|---|------|------|
| 9 | `static/index.html` | 全局变量加const/let、JSON.parse加try-catch |
| 10 | `static/index.html` CSS | 合并重复、右键边界检测 |
| 11 | `search_service.py` | 加载search_index.json、修复拼音匹配 |
| 12 | 数据 | 修复4个失败板块、清理weekly、统一源 |

### Phase 4：架构（远期）

| # | 操作 |
|---|------|
| 13 | 拆分为独立 JS/CSS |
| 14 | 统一数据源（SQLite or CSV）|
| 15 | 移除遗留 `cache_manager.py`、`sqlite_repo.py` |
| 16 | API版本前缀 |

---

## 六、数据流图

```
AKShare ──→ data_loader.py ──→ SQLite ──→ services/ ──→ Flask API ──→ 前端
              │                                         │
              ↓                                         ↓
           CSV文件(双份)                              KLineChart Pro
                                                       │
QMT ──────→ qmt_client.py                              │
              │                                         │
              ↓                                         ↓
           子进程调用                              MCP/AI技能(空壳)
```

**建议合并点**：统一数据源为SQLite(kline.db)，废弃CSV双份拷贝

---

## 附：关键文件清单

| 文件 | 行数 | 作用 |
|------|------|------|
| `static/index.html` | 1517 | 主入口（CSS+HTML+JS混合）|
| `app.py` | ~250 | Flask入口 |
| `data_loader.py` | ~400 | 统一数据加载 |
| `data_update_manager.py` | ~200 | 增量更新 |
| `search_service.py` | ~150 | 搜索逻辑 |
| `qmt_client.py` | ~150 | QMT接口 |
| `port_manager.py` | ~100 | 端口分配 |
| `signals_store.py` | ~100 | 信号持久化 |
